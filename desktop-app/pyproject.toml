[project]
name = "website-blocker-desktop"
version = "0.1.0"
description = "Desktop management interface for website blocker"
requires-python = ">=3.14"
dependencies = [
    "pywebview[gtk]>=6.1",
    "requests>=2.32.5",
]
